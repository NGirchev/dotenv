<?xml version="1.0"?>
<ruleset name="Custom PMD Ruleset"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_2_0_0.xsd">

    <description>Custom PMD ruleset with adjusted thresholds</description>

    <!-- Include best practices rules -->
    <rule ref="category/java/bestpractices.xml"/>

    <!-- Include code style rules, but exclude AvoidFinalLocalVariable -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="AvoidFinalLocalVariable"/>
    </rule>

    <!-- Include design rules with adjusted complexity thresholds -->
    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter"/>
        <exclude name="ModifiedCyclomaticComplexity"/>
        <exclude name="StdCyclomaticComplexity"/>
        <exclude name="CognitiveComplexity"/>
    </rule>

    <!-- Include error prone rules, but exclude DataflowAnomalyAnalysis -->
    <rule ref="category/java/errorprone.xml">
        <exclude name="DataflowAnomalyAnalysis"/>
    </rule>

    <!-- Include performance rules -->
    <rule ref="category/java/performance.xml"/>

    <!-- Include security rules -->
    <rule ref="category/java/security.xml"/>

</ruleset>

